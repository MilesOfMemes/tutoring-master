import React from 'react';

const Contact = ({handleLogout}) => {
    return(
        
        <section className="contact">

            <h2>Contact Us</h2>
            
        </section>
    );
};

export default Contact;
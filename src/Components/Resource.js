import React from 'react';

const Resource = ({handleLogout}) => {
    return(
        
        <section className="resource">

            <h2>Resource Page</h2>
            
        </section>
    );
};

export default Resource;
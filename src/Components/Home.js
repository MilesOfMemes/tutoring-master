import React from 'react';

const Home = ({handleLogout}) => {
    return(
        
        <section className="home">

            <h2>Home</h2>
            
        </section>
    );
};

export default Home;